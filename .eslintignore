# ESLint Ignore File (.eslintignore)
# Specifies directories and files to be ignored by ESLint.
# Note: This format is for older ESLint setups or when not using Flat Config.
# For ESLint v9+ with Flat Config (eslint.config.js), use the 'ignores'
# property within the config file for better control, including negations (!).

# Dependency Directories
node_modules/
.yarn/

# Build Output & Cache
dist/
build/
out/
output/
*.tsbuildinfo

# Test, Coverage & Documentation Output
coverage/
.nyc_output/
*.lcov
.vitest-cache/
docs/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment Variables Files
# WARNING: Negation (!) is not reliably supported in .eslintignore.
# Ignoring .env* here will likely ignore .env.example as well.
# Use Flat Config 'ignores' in eslint.config.js for precise control.
.env
.env.*

# Generated Code
prisma/generated/

# OS / Editor specific files
.DS_Store
Thumbs.db
# WARNING: Ignoring .vscode/ here will ignore potentially shared settings too.
.vscode/
.idea/
.history/

# Tooling Caches & Output
.npm/
.eslintcache
.cache/
*.tgz

# Configuration files (often excluded as they might use different syntax/globals)
vite.config.ts
vitest.config.ts
*.config.js
*.config.cjs
commitlint.config.*
prettier.config.*
# eslint.config.js # Usually ignored by ESLint anyway

# Other common ignores
.husky/
.turbo/
*.d.ts # TypeScript definition files

# Add any other project-specific directories or files to ignore here
# e.g., public/
# e.g., static/
